# Rent Manager E2E Test Environment Configuration

# Application URLs
BASE_URL=http://localhost:3000
API_BASE_URL=http://localhost:5000/api

# Start server before tests (set to 'true' if servers are not already running)
START_SERVER=false

# Browser settings
HEADLESS=true

# Zitadel OAuth Configuration (for authentication testing)
ZITADEL_AUTHORITY=https://rent-manager-txkjry.us1.zitadel.cloud
ZITADEL_CLIENT_ID=343292464775381118
ZITADEL_REDIRECT_URI=http://localhost:3000/auth/callback

# Test User Credentials (configure these for your test environment)
# Property Owner
TEST_PROPERTY_OWNER_EMAIL=owner.test@rentmanager.com
TEST_PROPERTY_OWNER_PASSWORD=TestPassword123!

# Renter/Tenant
TEST_RENTER_EMAIL=renter.test@rentmanager.com
TEST_RENTER_PASSWORD=TestPassword123!

# Admin
TEST_ADMIN_EMAIL=admin.test@rentmanager.com
TEST_ADMIN_PASSWORD=TestPassword123!

# Database Configuration (for test data setup/teardown)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rentmanager_test
DB_USER=postgres
DB_PASSWORD=your_password_here

# Stripe Test Configuration (use Stripe test keys)
STRIPE_TEST_PUBLIC_KEY=pk_test_your_key_here
STRIPE_TEST_SECRET_KEY=sk_test_your_key_here

# Test Data Configuration
ENABLE_TEST_DATA_CLEANUP=true
ENABLE_DATABASE_SEEDING=true

# CI/CD Configuration
CI=false

# Timeout Configuration (milliseconds)
DEFAULT_TIMEOUT=30000
LONG_TIMEOUT=60000
SHORT_TIMEOUT=10000

# Parallel Execution
MAX_WORKERS=4

# Retry Configuration
RETRY_COUNT=2

# Screenshot and Video
CAPTURE_SCREENSHOT=only-on-failure
CAPTURE_VIDEO=retain-on-failure
