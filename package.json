{
  "name": "rent-manager-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev:frontend",
    "dev:frontend": "npm run start --workspace=frontend",
    "dev:backend": "cd backend/RentManager.API && dotnet run",
    "dev:both": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "cd backend/RentManager.API && dotnet build",
    "test": "npm run test:frontend && npm run test:backend",
    "test:frontend": "npm run test --workspace=frontend",
    "test:backend": "dotnet test",
    "install:all": "npm install && cd backend/RentManager.API && dotnet restore",
    "clean": "rm -rf node_modules frontend/node_modules frontend/build && dotnet clean"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
